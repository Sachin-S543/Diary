{
    "name": "@secret-capsule/types",
    "version": "0.0.0",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "lint": "eslint .",
        "build": "echo 'No build needed — types-only package'",
        "test": "echo 'No tests in package'"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}