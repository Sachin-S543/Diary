{
    "name": "@secret-capsule/types",
    "version": "0.0.0",
    "types": "./src/index.d.ts",
    "scripts": {
        "lint": "eslint .",
        "build": "echo 'No build needed — types-only package (.d.ts)'",
        "test": "echo 'No tests in package'"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}